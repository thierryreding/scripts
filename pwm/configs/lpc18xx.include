kconfig architecture arm
kconfig allnoconfig
# basic configuration
kconfig enable DEBUG_FS
kconfig enable SYSFS
# architecture configuration
kconfig enable ARCH_LPC18XX
# PWM drivers
kconfig enable PWM
kconfig enable PWM_LPC18XX_SCT
# PWM users
include users.include
